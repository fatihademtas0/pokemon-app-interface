{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./", // Kök klasörü temel al
    "paths": {
      "@services/*": ["services/*"] // services klasörü için alias
    }
  }
}
